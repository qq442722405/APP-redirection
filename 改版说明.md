# APP 窗口启动器 · SIMO 语音启动接入版

最新增加「设置 → SIMO 语音启动」。支持 APP 范围、别名、开机恢复、后台重连，并沿用主界面 APP 预设。原软件 VISO 接口分析依据、限制和实车测试步骤见 `SIMO功能分析与测试说明.md`。本版尚未进行车机验证。

## 本次修改

- 删除「主界面壁纸」及旧配置的加载、导入和图片选择入口。
- 主界面「悬浮窗选位」、设置中的「悬浮窗口设置」，只在 Android 已授予悬浮窗权限时显示。返回程序时重新检查；权限被关闭时，已打开的悬浮设置及子页面会关闭。
- 「界面选项」增加紫色、黑色、灰色主题，默认紫色。选择后点击保存，主界面、卡片、按钮、设置窗口、开关和悬浮窗口跟随改变。选择取消不会保存；重启后保留已保存主题。
- 「添加悬浮按钮」「单图标操作」「手势APP选择」改用相同的 1000 × 700 JSON 布局和主题。添加后立即刷新悬浮按钮列表；手势操作沿用即时保存。
- 删除主界面“离上80 / 离下120”的两个可见框，保留上下留白和其他控件坐标。
- 保留上一版每个 APP 下方的独立预设框及 160 DPI 像素字号处理。

设计器增加三色主题预览和「预览：已授予悬浮窗权限」开关。权限开关只影响整页预览，编辑画布仍保留入口，方便调整位置。主题预览不会替你修改 APP 的设置；APP 中仍需选择主题并保存。

新增三份配置：添加悬浮按钮.json、单图标操作.json、手势APP选择.json。另新增 SIMO语音启动.json，总计 12 个页面。

本次新增：每个 APP 卡片下方都有预设选择框。点框选择预设后自动保存；双击上方 APP 图标/名称，按选定预设的窗口位置、宽高和启动模式运行。选「直接启动」则不指定窗口。

每个 APP 单独保存选择，重启后保留。预设改名、移动顺序不会改变关联。删除所选预设后，框内显示「预设已删除，请重选」，双击会要求重选，不会误用同名预设。旧版按名称保存的关联会在首次识别时迁移为固定 ID。系统操作按钮卡片不需要窗口预设。

APP 卡片高度由 200 调整为 150，下面增加 200 × 40 的选择框，间隔 10；APP 区域总高度和底部操作栏位置不变。新框使用 22px 字号。多排 APP 滚动后选预设/点 APP 会保留列表滚动位置。

## 字号按 DPI 160 统一

网页导出的 `fontSize` 是 CSS px，现明确映射为 Android `COMPLEX_UNIT_PX`，不当作默认 sp。160 DPI、系统字体默认比例时数字相等，因此原设计 16、20、35、40、55、60 等字号保持原数值，无需乘 1.6 或按电脑显示器 DPI 再换算。

| 网页设计字号 | Android 设计字号 | 160 DPI、默认字体时等效 sp |
| --- | --- | --- |
| 16px | 16px | 16sp |
| 20px | 20px | 20sp |
| 22px（新预设框） | 22px | 22sp |
| 35px | 35px | 35sp |
| 40px | 40px | 40sp |
| 55px | 55px | 55sp |
| 60px | 60px | 60sp |

所有自定义 Java 字号调用已集中到 `DesignTypography.setPx`，包括旧弹窗、悬浮服务、输入框、下拉已选项与展开列表。APP 内部上下文固定系统字体倍率为 1，不修改车机系统设置；需要放大时可使用 APP 的「界面选项 → 字体大小」。JSON 界面实际字号 = 设计字号 × 页面等比缩放 × APP 字体倍率。电脑浏览器缩放、显示器像素密度和中英文字体本身仍可能带来观感差异。

依据：[Android TextView 字号单位](https://developer.android.com/reference/android/widget/TextView)、[160 DPI 与 dp/sp](https://developer.android.com/training/multiscreen/screendensities)。

在提供的 8 份 JSON 基础上增加 3 个子页面，重排原生 Android 界面。主界面设计区为 2032 × 960，其他 10 个窗口为 1000 × 700。原工程包名、签名文件、APP 启动和悬浮服务保留。此工程并非 WebView 套壳。

## 调整界面的步骤

1. 双击 `design/程序界面设计.html`。下拉框可直接打开 12 套内置设计，也可以用「载入配置」打开 `app/src/main/assets/layouts` 内的同名 JSON。
2. 拖动控件或修改左距、上距、宽度、高度、字号。用「整页预览」检查整体效果。
3. 点击「保存配置」。保留导出的原文件名，将文件替换到 `app/src/main/assets/layouts`。
4. 在工程根目录执行 `node tools/validate-layouts.cjs`，然后重新编译 APK。

网页切换页面会暂存当次修改；关闭或刷新网页会丢失尚未导出的修改。内置页面是初始设计副本，后续修改应重新载入已保存的 JSON。网页不会直接改写 Android 工程文件。

## 坐标与控件规则

- `left/top/width/height/fontSize` 都按设计像素解释。`globalDpi` 与原网页一致，仅供毫米、dp 换算，不再额外缩放控件。
- 原生界面按可用区域等比缩小，默认不放大。主界面从可用内容区域左上角开始；设置窗口居中。视觉位置和原生触摸位置使用同一组测量坐标，不修改触摸事件。
- 顶部 80、底部 120 是主界面设计内的留白，随整个设计等比缩放；系统状态栏/导航栏仍由 Android 管理。
- `id: e0/e1/...` 是功能绑定标识。修改位置和文字不会改变功能。主界面的 e32–e39 是 8 个 APP 预设框。保留已有 ID，不删除原有控件或手动改变类型。复制出的新按钮不会自动获得原生业务功能，需要在 Java 中新增绑定。
- 原生输入框、开关、下拉框替换网页内的占位按钮；系统控件外观与网页占位块不会完全相同。窗口名称是编辑辅助文字，原生界面不显示「窗口 #47」等标记。
- APP 与预设卡片使用真实数据；空列表显示添加提示。主界面默认每排 8 个 APP，超出部分滚动；预设横向滚动。添加 APP 列表按 5 列排列，超过两排可滚动。

## 功能与兼容

- 主界面 APP 单击选中，双击启动；预设和 APP 长按保留移动/删除功能。
- 添加 APP 页面支持搜索、用户/系统/全部/按钮分类、默认预设、复制/粘贴、取消/保存；「无操作」清空当前选择。此处选定的默认预设与主界面 APP 下方的预设框同步，按固定 ID 关联。
- 系统按钮卡片单击执行相应动作，不会覆盖当前选中的 APP；需要原来的无障碍权限。
- 预设编辑保留原工程全部 6 种模式。设计中没有模式 5，放在同一行最右侧，避免旧预设的功能丢失。原设计「长度」对应窗口高度。
- 设置页新增设计中的「开机任务」总开关。自动任务页面右上角保留「添加任务」入口；列表可滚动。
- 界面选项中的「字体大小」「界面大小」「每排数量」使用独立的新布局设置，默认均按本次设计。界面大小受屏幕可用区域限制；超过可用范围时仍等比缩小。旧版字体、dp 缩放不会叠加到本次布局。
- 悬浮设置中的方向、形状、开关及数值在保存时应用；手势选择、按钮添加/删除仍沿用原工程的即时保存行为。保存悬浮设置后窗口保持打开。
- 权限检查、配置导入/导出、悬浮服务沿用原工程。系统是否允许第三方 APP 多窗口、窗口移动及后台控制仍由目标设备决定。

## 工程入口

- `DesignSurface.java`：读取 JSON、原生控件测量与等比布局、滚动区域。
- `LauncherDesign.java`：主界面、设置、界面选项、添加 APP、自动任务功能绑定。
- `MainActivity.java`：保留业务逻辑；预设编辑、悬浮设置、权限诊断接入 JSON 布局。
- `app/src/main/assets/layouts/`：APK 实际读取的 12 份配置；包含 `fontUnit: px`、`targetDpi: 160` 标记。
- `ThemePalette.java` / `DesignTheme.java`：三种主题颜色、原生背景与已打开页面的刷新。
- `DesignTypography.java`：统一像素字号与 APP 字体上下文。
- `PresetSelection.java`：固定 ID/旧名称解析，避免重命名、移动或删除后误启动。

## 构建与验证

沿用工程已有环境：JDK 17、Gradle 8.7、Android SDK 35，执行 `gradle :app:assembleDebug`。也可使用原有 GitHub Actions 构建流程。工程原本未包含 Gradle Wrapper。

本次已通过：

- 12 份布局、273 个控件的 ID、尺寸与边界检查；新增 8 个预设框与卡片、底部按钮不重叠。
- 10 项预设关联回归检查：默认启动、选择 ID、改名、重排、重名、删除后同名替换、旧名称迁移、旧名称失效和空列表。
- 字号集中使用 px、DPI 160 配置标记、设计器与 APK 配置一致性检查。
- 全部 Java 源码的编译检查，使用 Android 35 和 AndroidX 实际依赖；此检查的资源 ID 使用生成的 R 类占位值。
- 原有页面和新增 SIMO 页面切换、绑定 ID 保留、三色主题切换与整页预览检查，未出现脚本错误。

尚未完成：完整 Gradle APK 打包、模拟器/车机运行与实际触控验证。本交付为源码工程，不包含已经验证可安装的 APK。

可重复执行 `node tools/validate-layouts.cjs` 和 `node tools/tests/check-design.cjs`。预设关联测试是独立 Java 主程序，可将 `PresetSelection.java` 与 `tools/tests/PresetSelectionTest.java` 一起编译后运行 `com.acc.acc.PresetSelectionTest`。
