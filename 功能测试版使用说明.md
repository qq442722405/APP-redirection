# APP窗口启动器 · 功能测试版

版本：1.2-Lab（versionCode 24），2026-09-19。

本版在原有界面、三色主题、APP预选窗口和SIMO启动功能基础上，新增“设置 → 功能测试”。这是源码工程交付，尚未生成 APK，也没有连接极越车机实测。可以使用工程中的 GitHub Actions“构建 APP窗口启动器”工作流生成 APK，或在 JDK 17、Gradle 8.7、Android SDK 35 环境构建。

## 测试入口

打开“设置 → 功能测试”，先点“选择测试 APP”。仅列出已安装、有启动入口的第三方 APP。测试窗口包括：

| 页面 | 测试内容 |
|---|---|
| 关闭 APP 测试 | 返回一次、连续返回两次、清理后台进程、强制停止接口、系统 APP 信息页 |
| 窗口与屏幕测试 | 左屏、中屏、右屏、后排屏、指定区域启动、自由窗口、窗口内显示 APP |
| SIMO 功能测试 | 当前 APP 的关闭、左中右/后排启动与移动、自由窗口启动口令 |
| 日志监听与报告 | 本 APP logcat、授权后的系统相关日志、显示器与权限诊断、导出报告 |

每次执行分配独立测试编号。观察效果后点“记录有效 / 记录无效”，或在测试首页标记“待定”。报告会把结果与测试编号、包名和参数关联。没有自动把任何实验升级成正式功能。

“返回”只关闭当前窗口，测试口令和已开启的监听可继续在后台运行。“结束测试”撤销测试口令、停止监听并取消尚未发送的返回操作；已经打开的目标 APP 不会自动被关闭。测试语音默认为关闭，进程重启后也不会自动开启，不参与开机任务。正式 SIMO 启动开关独立保留。

## 建议的实车测试顺序

1. 选一个第三方 APP，打开“日志监听与报告”，先运行“显示器与权限”，再开启“本 APP 日志”。
2. 测试关闭的各个按钮，每测一次就返回测试页标记结果。使用返回方式时，需要先在已有“权限与诊断”中启用无障碍服务。
3. 进入“窗口与屏幕测试 → 配置屏幕 / 窗口参数”。检查左、中、右的坐标和显示器编号，然后测试启动位置。
4. 后排先选择真实显示器 ID，再填写坐标和大小。默认后排未配置；系统没列出后排时，不会猜一个编号发起请求。
5. 配置“自定义窗口”，先试“指定区域启动”，再试“自由窗口测试”。
6. 最后试“窗口内显示 APP”。该实验只验证画面能否显示在内嵌区域，不支持触摸转发。
7. 按钮测试有结果后，再开启“SIMO 功能测试”中的开关，按页面列出的完整口令喊话。
8. 导出报告，并告诉我哪些测试编号有效、哪些无效以及实际现象；完成后点“结束测试”。

## 各种关闭方法的区别

| 方法 | 实際执行与判断 |
|---|---|
| 返回一次 | 若目标不在前台，先请求打开目标，最多等待4秒的无障碍前台事件，然后发送一次返回；无法确认时不发送。 |
| 连续返回两次 | 第一次返回后等待600毫秒，仍确认目标在前台才发第二次。更换目标、新测试或结束测试都会取消待执行的旧操作。 |
| 清理后台进程 | 调用 `killBackgroundProcesses`。不等于强行停止，不保证关闭前台窗口，也不阻止 APP 再次运行。 |
| 强制停止接口 | 尝试 `forceStopPackage`。普通安装通常受隐藏接口或权限限制；拒绝原因写入日志，没有自动提权。 |
| 系统 APP 信息页 | 打开指定 APP 的系统管理页面，由你手动选择系统提供的“强行停止”。 |

返回也可能只是退出 APP 内部的一层页面，不能仅凭接口返回判断已关闭进程。相关语义参考 Android 官方 [ActivityManager 文档](https://developer.android.com/reference/android/app/ActivityManager)。

## 窗口、左中右和后排

- 左中右默认按主显示器宽度三等分，这是供测试调整的初始值，不代表已识别车机分区。请先核对车辆实际坐标。
- 每个位置独立保存 `Display ID / X / Y / 宽 / 高`，单位是车机像素。切换显示器时先填充该显示器的全屏尺寸，再自行调整。
- 后排使用实际 `Display ID`，不复用正式启动代码中只使用默认显示器的路径。
- 参数越界、显示器已断开或系统不允许投放时，明确记录错误；没有悄悄改坐标或退回普通全屏启动。
- “指定区域启动”使用 `ActivityOptions` 的显示器与窗口边界参数。“自由窗口”另外尝试 freeform 模式。系统可能忽略边界，或复用已有 APP 任务；口令中的“移动”同样是重新发起带目标区域的启动请求，并非已确认具有任务搬移权限。
- “窗口内显示 APP”创建本 APP 的虚拟显示器，将画面投到实验窗口中的显示区域（1280×720、160 dpi），随后尝试把目标 APP 启动到该显示器。关闭实验窗口时释放虚拟屏。系统/APP 若不允许，可能拒绝、黑屏或仍开到主屏；请据实标记。
- 内嵌实验没有实现触摸注入、跨 APP 手势转发或特权 TaskView 控制。它目前用于验证能否显示画面，不能视为已经完成可交互的容器功能。

显示器和窗口参数的限制参考 Android 官方 [ActivityOptions 文档](https://developer.android.com/reference/android/app/ActivityOptions)、[DisplayManager 文档](https://developer.android.com/reference/android/hardware/display/DisplayManager)。

## SIMO 实验口令

例如选中 APP“音乐”，可以喊：

- 关闭音乐
- 把音乐移到左屏 / 把音乐移到中屏 / 把音乐移到右屏 / 把音乐移到后排屏
- 在左屏打开音乐 / 在中屏打开音乐 / 在右屏打开音乐 / 在后排屏打开音乐
- 窗口打开音乐

页面的“关闭方式”决定关闭口令调用哪项实验；默认连续返回两次。别名仅用于测试，不修改正式 SIMO 别名。当前只对选中的一个 APP 登记这些实验口令。

“已发送口令”只表示向 VISO 提交登记请求。没有回调时，请在报告中注明；有回调但没有启动或移动时，查看操作日志及车机后台启动限制。测试期间建议停用原软件中的同名口令，避免互相覆盖。

实验回调继续校验 VISO 调用 UID、页面、动作和本地口令名单；关闭实验、切换 APP 后拒绝旧目标的口令。正式“打开 APP”保留原有稳定标识与预选窗口逻辑。

## 日志与反馈

操作日志进入测试窗口后自动记录，logcat 监听需要手动开启。记录只在本机保存，不自动发送。内存最多保留1000条；磁盘约1MB滚动保留当前及上一份日志，下一次打开测试窗口会读取当前日志的最后1000条。界面显示最近30条，导出的是当前保留记录（最多1000条），不是无限历史。

记录含义：`REQUEST` 是请求；`API_RETURN` 是方法返回；`OBSERVED` 是观察到前台 APP 事件；`ERROR` 是异常或拒绝；`MANUAL` 是你的实车结论。前台事件不能证明窗口坐标或进程退出。

“系统相关日志”需要车机实际授予 `READ_LOGS`，只在清单声明不会自动获得授权。未获授权时按钮会提示不可读取，可继续使用本 APP 日志。若设备已有可用 ADB，可在连接车机的电脑上尝试：

```text
adb shell pm grant com.acc.acc android.permission.READ_LOGS
```

是否允许由车机固件决定；拒绝时保留错误即可。测试结束后如需撤销：

```text
adb shell pm revoke com.acc.acc android.permission.READ_LOGS
```

系统日志按窗口/任务、SIMO/VISO、目标包名及权限拒绝等信息筛选，不保证覆盖厂商全部日志。没有输出也不能推断操作成功。参考 Android 官方 [日志访问限制](https://developer.android.com/privacy-and-security/risks/log-info-disclosure)。

导出时通过系统文件保存器选择位置，内容包括设备版本、实际显示器列表、测试坐标、保留的日志和人工标记。反馈格式可用：`测试编号 / APP / 按钮或口令 / 实际在哪块屏 / 是否关闭或显示 / 补充现象`。

## 网页设计与验证范围

设计器新增“功能测试、关闭APP测试、窗口屏幕测试、SIMO功能测试、测试日志”五个页面；设置页新增稳定控件 `e15`。可继续加载同名 JSON 调整位置、尺寸和字号，保存后放回工程 `app/src/main/assets/layouts/` 再构建。新页面采用160 dpi设计像素，沿用三色主题。APP选择器、参数编辑器和内嵌实验窗口为原生动态界面，当前不作为独立 JSON 模板。

另外修正了网页文本框忽略 JSON 宽高、换行显示不一致的问题。浏览器预览只是布局预览，不会在电脑上实际关闭或启动 Android APP。

本版已完成：25项语音口令逻辑检查、16项窗口边界检查、10项原有预设关联检查；17份布局的控件ID/边界及160 dpi字体检查；设计器内置配置一致性检查；设置和五个新增页面浏览器预览；全部 Java 源码编译检查。

Java 检查使用 Android 35、实际 AndroidX 依赖及占位 R 类，不能替代完整 Gradle 构建。未完成 APK 打包、Android 原生运行、真实 VISO 往返和实车测试；请把本版视为待车机验证的实验实现。
