# SIMO 关闭 APP 专项测试版

版本：1.5-SimoCloseTest，versionCode 27。入口：**设置 → SIMO 语音控制 → 关闭 APP 测试**。这是修改后的源码工程，不包含已打包 APK，仍需用工程的构建流程生成安装包。

## 建议按这个顺序测试

1. 在 SIMO 设置中打开「语音控制总开关」和「允许关闭 APP」，保存。检查登记范围包含目标 APP，且没有在「APP 口令／别名」中排除它。
2. 打开专项测试页，选择一个第三方 APP，把「语音关闭测试方式」切成「只记录语音，不执行关闭」。点「打开 APP」，然后对 SIMO 说「关闭＋APP 名称」或「关闭＋别名」。
3. 回到测试页查看记录。正常应先出现「收到 VISO」，再出现「匹配关闭」和「已收到关闭口令」。此模式故意不关闭 APP。
4. 再逐个尝试手动「返回一次」「返回两次」「三指下滑」「慢速三指」。每次按钮会打开所选 APP，并在 5 秒后执行一次。需要启用启动器的无障碍服务。返回操作只在目标 APP 是活动窗口时发送；第二次返回前会再次确认目标。
5. 找到手动有效的方式后，在「语音关闭测试方式」中选择同一种，再喊语音口令。若手动三指有效、语音三指无效，再试「延迟 2 秒后三指下滑」，排查语音面板未收起而遮挡目标的情况。
6. 必要时单独测试「中屏回桌面」「右屏回桌面」。这两个按钮操作整个区域，点击后会说明范围并让你确认执行，效果不限定于选中的 APP。
7. 每组测试结束点「复制结果」，把文字粘贴反馈，并补充实际看到的效果，例如“返回两次有效，慢速三指没有效果”。测试后可点「语音恢复原方式」。

「取消待执行」用于取消尚未发送的手动倒计时；关闭测试页或更换测试 APP 也会取消该倒计时。已经提交的系统动作不能撤回。语音延迟测试可通过「语音恢复原方式」或关闭并保存语音开关取消尚未发送的动作。

## 可以对比的方式

| 方式 | 作用与限制 |
| --- | --- |
| 只记录语音 | 验证 VISO 回调和口令匹配，不发关闭动作 |
| 返回一次 | 向当前活动的目标 APP 发一次返回，可能只返回上一级 |
| 返回两次 | 两次间隔 500ms，目标已离开活动窗口时取消第二次 |
| 三指下滑 | 在目标可见窗口内执行 380ms 三指向下手势 |
| 慢速三指 | 同一滑动范围，改为 900ms，用于对比车机识别速度 |
| 延迟后三指 | 语音匹配后等 2 秒，再检查目标并执行 380ms 手势 |
| 中屏／右屏回桌面 | 通过 CAP 发送区域 HOME 请求，仅提供原 APK 中已确认的区域编号 |

语音测试方式只临时影响选中的一个 APP，其他 APP 保持原三指方式。更换测试 APP 会清除之前 APP 的临时方式；进程退出后也恢复默认。不会自动把多个关闭方案轮流执行，也不会自动把区域 HOME 接到正式语音命令中。

上述动作是收起窗口、返回或回桌面，不等于强制停止后台进程。系统接受返回、完成手势或 CAP 返回数据，都不能单独证明目标已关闭，需要结合实际屏幕效果判断。

## 怎样读结果

| 观察 | 优先检查 |
| --- | --- |
| 喊话后没有新增「收到 VISO」 | 语音服务连接、登记范围、别名冲突，以及车机是否把口令回调给启动器 |
| 收到 VISO，但没有匹配关闭 | 日志中的命令 ID、口令、关闭开关及页面／动作校验原因 |
| 目标窗口数为 0 或大于 1 | APP 是否可见、是否开了多份窗口，以及无障碍窗口读取能力 |
| 目标不是活动窗口 | 返回测试未发送，先把目标放到前台；语音面板也可能影响活动窗口 |
| 窗口被遮挡、手势跨区域或坐标无效 | 检查窗口位置，尝试延迟三指，并查看「窗口检查」记录 |
| 手势已完成但窗口没变化 | 当前固件可能没有把该手势作为窗口关闭，继续比较其他方案 |
| CAP 连接失败／权限拒绝／超时 | 该固件可能不支持普通第三方应用访问；不会自动重试区域操作 |

回调和匹配计数是当前进程累计值；「清空记录」清空最近记录，不重置累计计数。记录只保存在本进程内，最多 60 条，不监听系统 logcat、不录音、不上传。进程退出会丢失记录，请先复制再重启。

## 本次同时修正

- 原三指范围检查要求整个窗口完全位于估算的三分屏内，会拦截已有「中 上-80」等稍微跨过分界线的预设。现在检查真正的三指滑动范围，保留窗口越界、遮挡和目标不唯一的拦截。
- 返回操作根据无障碍的实际活动窗口确认目标，避免仅凭最近一条通知／其他无障碍事件的包名判断。
- 语音重复回调沿用前一次结果码，不再把前一次失败的重复请求回报为成功。

## 区域接口依据

来自用户上传原 APK 的静态分析：`i32.g` 使用 `SystemUI / Common / externalControl`，参数为 `action=ACTION_HOME` 和 `area`；`v05` 仅确认 `CENTER=1`、`RIGHT=2`。`dz.b`、`r34.writeToParcel` 提供 Binder 事务 1 的序列化格式。

该测试按需连接 `com.jidu.capservice/com.jidu.capservice.CapService`，发送一次用户选择的区域命令，结束后解除绑定。10 秒未返回时记录效果未知，不重复提交。没有恢复已删除的车状态悬浮窗、车辆数据轮询或其他车控操作；没有猜测左屏或后排区域编号。

## 验证范围

已通过全部 Java 源码编译检查、13 页共 298 个控件的布局检查、XML 解析、HTML 内置配置一致性检查及测试页浏览器预览。65 项 JVM 断言通过：语音口令 23、预设关联 10、窗口／手势边界 23、测试方式与日志 9。

源码编译检查使用 Android 35、AndroidX 依赖和占位 R 类，不能替代完整 Android 构建。本次没有进行 Gradle APK 打包、车机语音、无障碍手势或 CAP Binder 实机验证，交付的是便于实车逐项比较和反馈的测试版。
