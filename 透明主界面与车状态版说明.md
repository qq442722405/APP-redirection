# APP 窗口启动器：透明主界面与车状态版

版本：1.3-Vehicle（versionCode 25）。本次交付包含修改后的源码工程、网页设计器和 13 份 JSON 配置，不包含已经打包的 APK。原始上传文件及之前的交付包未改动。

## 界面改动

- 主界面背景透明，取消外层边框；APP 卡片、预设选择框和操作按钮保留各自样式。
- 「设置 → 界面选项」增加「隐藏主界面窗口预设」。保存后隐藏顶部预设区，APP 区域向上扩展。每个 APP 下方的独立预设选择框仍保留，双击 APP 仍按所选预设启动。
- APP 按可用宽度自动换行；「每排 APP 数量上限」只限制每排最大数量。超过第二排继续排列，并可上下滚动。
- 设置页、添加页、确认框、列表和预设下拉选择统一在主 Activity 内显示，使用实际布局坐标处理触控，去掉多窗口缩放造成坐标不一致的路径。尚需在车机上验证实际触控、滚动和键盘输入。
- 继续保留 160 DPI 的 px 字号映射、紫/黑/灰三色主题及已有悬浮权限控制。
- 删除整组「功能测试」入口、实验页面、日志监听服务和 READ_LOGS 权限。正式的「权限与诊断」保留。

## SIMO 关闭 APP

入口为「设置 → SIMO 语音控制」。开启语音总开关及「允许 SIMO 关闭 APP」，选择登记范围，保存；随后可以说「关闭＋APP 名称」或「关闭＋别名」。别名和 APP 开关沿用原有语音登记设置。需要启用本启动器的无障碍服务；升级后若服务没有获得窗口读取能力，可关闭再开启该无障碍服务。

这次根据原 APK 的可见窗口关闭逻辑重新实现：查找目标 APP 当前可见的窗口，在其边界内执行三指向下手势。不通过启动器先切到前台，因此减少遮挡目标窗口。目标不存在、有多个匹配窗口、手势区域被上层窗口遮挡或边界不明确时返回失败，不猜测目标。启动器自身和系统组件不作为关闭目标。

这里的「关闭」指请求车机收起/关闭可见窗口，不代表强行结束 APP 后台进程。手势完成回调也不代表 APP 已被系统强制停止。原 APK 另有区域 HOME 的 CAP 分支，本版未接入该分支；实车是否响应三指手势、VISO 是否接受关闭口令，仍需验证。

建议先保留一个容易辨认的第三方 APP 在单个屏幕中显示，喊关闭口令，再到语音设置查看最近请求结果。原软件若也登记同名口令，请先关闭其中一方的同名登记，便于判断。

## 车状态悬浮窗口

入口为「设置 → 车状态悬浮窗口」。先授予悬浮窗权限，再开启并保存。支持选择显示项目、16–48 px 字号、20–100% 不透明度、拖动标题移动、锁定位置以及重置位置。通知栏也可关闭悬浮窗。

已接入的只读项目：电量、实际剩余续航、CLTC 续航、车速、挡位原始代码、四轮胎压、电池温度。胎压按原 APK 的换算逻辑由 kPa 转为 bar。挡位映射未经确认，暂显示原始代码。

数据接口来自上传 APK 的实际 CAP 调用：绑定 `com.jidu.capservice/com.jidu.capservice.CapService`，使用 `service.intent.action.CAP`、`com.jidu.capservice.aidl.CapServiceAidlInterface` 和事务 1。读取 `carsettings` 下的 `power`、`vehiclemode`、`assisteddriving`、`battery` 分组；只发送读取请求，不发送车辆控制命令。原软件的 `si`、`dz`、`r34`、`qi`、`dq0` 等类提供了字段、序列化和单位换算依据。

后台约每 3 秒轮询一次所选项目；界面每秒更新显示。接口缺失、权限拒绝或字段无效显示「未获取」并记录原因；超过 15 秒未更新的有效读数显示「数据过期」。真实的零值允许显示，不使用演示数据代替读取结果。车机固件可能限制第三方调用，尚未验证车辆上的 Binder 返回。

Android 12 锁定穿透模式会把窗口不透明度限制在系统允许上限内；多个悬浮窗重叠时，系统仍可能因合计遮挡程度阻止穿透。依据：[Android FLAG_NOT_TOUCHABLE 规则](https://developer.android.com/reference/android/view/WindowManager.LayoutParams#FLAG_NOT_TOUCHABLE)。

本版复现的是原软件车况悬浮窗的上述只读核心能力，没有打包原 APK 的 DEX、图片或库，也没有复制其全部车控功能。

## 网页设计器与 JSON

打开配套 HTML 即可编辑内置页面或加载 JSON。新增「预览：隐藏窗口预设」及「预览 APP 数量」；主界面预览中的棋盘格代表透明背景，不会成为 APP 背景。模拟 APP 数量默认 16，便于检查换行。

字号继续以设计像素存储，Android 用 px 应用，并随整页布局统一缩放。160 DPI 下不需要把字号乘以 1.6。网页主题、权限和隐藏预设开关是预览状态；实际 APP 需在对应设置中保存。

主界面 APP 排列由可用区域、每排数量上限和卡片模板动态计算；`e9` 控制卡片宽高，`e32` 控制预设选择框高度。调整其他示例 APP 卡片的位置不会覆盖运行时自动换行。SIMO 配置文件名继续使用 `SIMO语音启动.json`，界面标题已改为「SIMO 语音控制」。

## 已验证与待验证

已通过全部 Java 源码编译检查、13 页 JSON 的控件检查、XML 解析、网页内置配置一致性检查及浏览器预览。52 项 JVM 断言通过：语音口令 23 项、预设关联 10 项、换行/关闭范围/车辆数据解析 19 项。

Java 编译检查使用 Android 35、AndroidX 依赖及占位 R 类，不能替代完整 Android 构建。本次未完成 Gradle APK 打包，也没有在 Android 设备或车机上运行。因此触控修复、SIMO 响应和车辆数据读取属于已实现、待实车验证的功能。

工程保留 GitHub Actions 构建流程，使用 JDK 17、Gradle 8.7、Android SDK 35 构建 Debug APK。安装后优先验证：隐藏预设后的换行、各设置页触控、SIMO 打开/关闭一个 APP、车况数据是否真实更新。
