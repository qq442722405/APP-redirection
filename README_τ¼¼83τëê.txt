第83版修复说明

1. 恢复“自动启动任务”入口：设置 -> 自动启动任务。
   - 可添加多个 APP。
   - 可设置任务间隔。
   - 支持选择窗口预设。
   - 保存后由 BootReceiver 在开机广播中执行。
   - 原有开机启动、本 APP 开机启动和开机延迟保持不变。

2. 修复悬浮窗口开关逻辑：
   - 开启前检查“显示在其他应用上层”权限。
   - 未授权时自动打开对应系统授权页面。
   - 授权返回 APP 后再次开启即可启动悬浮服务。
   - 悬浮服务启动时再次检查权限，避免无权限导致服务立即失效。
   - 保留横向/竖向方向切换、悬浮 APP 列表、添加 APP 等原功能。

3. 保留第82版已有功能：
   - 主界面字体大小 50-300%
   - 主界面界面大小 50-300%
   - 三列 APP 选择滚动
   - 弹窗左右边距设置
   - 新建窗口预设复制/粘贴
   - 触控位置修复方案
   - 所有弹窗不透明

构建：clean :app:assembleDebug
APK：app/build/outputs/apk/debug/app-debug.apk
