# APP 窗口启动器 · 窗口恢复修复版

版本：1.4-WindowRestore，versionCode 26。本次提供源码工程、HTML 设计器和 12 份 JSON 配置，没有已打包 APK。

## 本次改动

1. 删除车状态悬浮窗口：菜单、页面、后台服务、CAP 读取代码、对应配置及车辆服务查询声明全部移除。升级后打开启动器会清理旧的车况设置键。原有普通悬浮按钮和 SIMO 语音功能保留。
2. 主界面不再显示「悬浮窗选位」。入口移到新建／编辑窗口预设页面右上方，获得悬浮窗权限后显示。打开选位框使用当前正在编辑的参数；确定后只回填左距、上距、宽度和高度，保留名称、分类及其他已填写内容。若当前为全屏模式，选位后自动切回模式 1，使所选宽高生效。取消选位不修改预设。
3. 修正返回／关闭后重新按预设打开时的启动路径，统一主界面、悬浮按钮、开机任务及 SIMO 使用的预设启动参数。

## 窗口恢复的修改依据

旧代码在关闭前唤起后台 APP 时使用 `RESET_TASK_IF_NEEDED`，且没有附带上次窗口参数；再次按预设启动又使用 `REORDER_TO_FRONT` 优先复用任务，异常处理还会去掉窗口选项再次普通启动。这些路径可能保留或产生全屏任务。

新代码在按预设启动时清除任务重置／前移等复用标志，使用 `NEW_TASK | MULTIPLE_TASK` 请求新任务，同时提交当前预设的窗口边界及显示器 ID。激活目标以执行返回／关闭时则保留任务，附带最近保存的窗口参数，不请求新实例。窗口启动抛出异常后显示失败原因，不再去掉参数普通启动。非全屏预设保存时要求宽高大于零。

此策略可能为支持多实例的 APP 创建额外任务，目的是避免直接恢复之前的全屏任务；不会清除 APP 数据或强制结束进程。Android 的 `singleTask`／`singleInstance` 等目标应用声明、不可调整大小的窗口以及车机自己的窗口策略，仍可能限制结果。依据：[Intent 启动标志](https://developer.android.com/reference/android/content/Intent)、[任务与返回栈](https://developer.android.com/guide/components/activities/tasks-and-back-stack)。

本次修正了启动器能控制的代码路径，没有在用户车机上验证，因此不能保证每个第三方 APP 均能被系统重新布局。

## 建议实车复测

1. 覆盖安装新构建的 APK，保留原有预设和 APP 列表。
2. 在「新建窗口预设」中填写名称，再打开「悬浮窗选位」，调整后确认，检查四个坐标／尺寸输入框及名称是否正确保留，点击保存。
3. 给同一个 APP 选择一个明显较小的预设，启动并检查窗口位置、大小；点「关闭」收起，再双击 APP 或点击预设，重复三次。
4. 再试一个不同位置／大小的预设以及悬浮快捷按钮。若仍全屏，请记录 APP 名称、所选预设、是否只有第二次异常，以及启动器显示的失败信息。无需开启已经删除的测试功能。

## 验证情况

已通过全部 Java 源码编译检查、12 页共 277 个控件的布局检查、JSON 与 HTML 内置配置一致性检查、XML 解析及预设页面浏览器预览。53 项 JVM 断言通过：语音口令 23 项、APP 预设关联 10 项、换行／关闭边界／窗口坐标 20 项。

源码编译检查使用 Android 35 和 AndroidX 依赖及占位 R 类，不等于完整 Android 构建。尚未执行完整 Gradle 打包、原生 Android 界面运行或车机重复开关窗口验证。工程保留 GitHub Actions 构建流程。

网页和运行时入口使用同一个新增控件 ID：`新建预设窗口.json` 的 `e46`。旧主界面 `e31` 和设置 `e16` 已删除；主界面其他 ID 保持不变。请使用这次配套的新版 JSON 和 HTML，避免旧配置把已删除入口重新画出来。
